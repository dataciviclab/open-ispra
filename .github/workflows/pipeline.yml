name: pipeline

on:
  pull_request:
    types: [closed]
    paths:
      - "datasets/**"
      - "support/**"
  schedule:
    - cron: "0 6 1 * *"
  workflow_dispatch:
    inputs:
      repo-slug:
        description: "Repo slug (es. open-ispra)"
        required: true
        type: string

concurrency:
  group: pipeline-${{ github.event.pull_request.number || github.ref || 'schedule' }}
  cancel-in-progress: false

jobs:
  pipeline:
    if: github.event.pull_request.merged == true || github.event_name != 'pull_request'
    uses: dataciviclab/.github/.github/workflows/pipeline-reusable.yml@main
    with:
      repo-slug: "${{ inputs.repo-slug }}"
      run-command: "make run-all"
    permissions:
      contents: write
      pull-requests: write
      id-token: write
    secrets: inherit
